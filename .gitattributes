# Normalize line endings: PowerShell scripts get CRLF (matches sibling projects),
# everything else stays LF. Without this, a contributor's core.autocrlf setting
# could flip endings on checkout and produce noisy diffs.
*.ps1 text eol=crlf
*.psm1 text eol=crlf
